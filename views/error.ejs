<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/output.css">
  <title>LY1 - Erreur</title>
</head>

<body class="flex flex-col mx-10 min-h-screen">
  <header>
    <nav class="flex justify-between py-10">
      <a href="/">
        <h1 class="text-3xl font-light tracking-tight">LY1</h1>
      </a>
    </nav>
  </header>

  <main class="flex-1 flex flex-col justify-center items-center text-center">
    <div class="max-w-md">
      <h2 class="text-2xl font-bold mb-4">Une erreur s'est produite</h2>
      <p class="text-gray-600 mb-6"><%= message %></p>

      <% if (process.env.NODE_ENV === 'development' && error && error.stack) { %>
      <details class="text-left">
        <summary class="cursor-pointer text-blue-600 mb-2">Détails techniques</summary>
        <pre class="bg-gray-100 p-4 rounded text-sm overflow-auto"><%= error.stack %></pre>
      </details>
      <% } %>

      <a href="/" class="button-blue-light mt-6 inline-block">Retour à l'accueil</a>
    </div>
  </main>

  <%- include('partials/footer') %>
</body>

</html>